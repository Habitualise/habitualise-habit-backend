package app.habitualise.habit_backend.domain.aggregates

import app.habitualise.habit_backend.domain.common.AggregateRoot
import java.util.*

class HabitStyle(override val id: UUID) : AggregateRoot<UUID>() {
    
}